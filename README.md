# Hometask9-Jenkins

To complete this task, I created Azure instance with and installed Jenkins on it via [terraform script](azure-instance.tf).
Then I installed necessary plugins and created jenkins user. 

